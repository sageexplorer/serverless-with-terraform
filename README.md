( I am pluggin in machine learning algo behind the scene, but for now, just IAC in it's simplest form )

To create:

```terraform init```

```terraform plan```

```terraform apply```

To destroy:

```terraform destroy```
